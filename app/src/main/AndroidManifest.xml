<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.legendsumeet.facerecognition" >

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_face"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/icon_face"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaceRecognition.NoActionBar" >
        <activity
            android:name=".sponsor"
            android:exported="false"
            android:label="@string/title_activity_sponsor"
            android:theme="@style/Theme.FaceRecognition.NoActionBar.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".splash_screen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FaceRecognition.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>